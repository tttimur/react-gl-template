Install
`yarn`

Start dev server
`yarn dev`

Notes
- Ignore shaders directory - was able to load in the glsl file through babel loader but wasn't working with react fast refresh. Created a suboptimal dev/iterating experiece